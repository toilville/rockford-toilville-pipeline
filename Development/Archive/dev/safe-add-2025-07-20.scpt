-- Safe Apple Contacts Add Script (Generated 2025-07-20)
-- Adding 980 new contacts that don't exist in Apple

tell application "Contacts"
  activate
  set addedCount to 0
  

  -- Adding: Yuko Kobayashi
  try
    set newContact to make new person with properties {first name:"Yuko", last name:"Kobayashi", organization:""}
    make new email at end of emails of newContact with properties {label:"work", value:"coovam5884@hotmail.co.jp"}
    set addedCount to addedCount + 1
  on error
    -- Skip if already exists
  end try

  -- Adding: Wm. Lindsay
  try
    set newContact to make new person with properties {first name:"Wm.", last name:"Lindsay", organization:""}
    make new email at end of emails of newContact with properties {label:"work", value:"wm.lindsay@gmail.com"}
    set addedCount to addedCount + 1
  on error
    -- Skip if already exists
  end try

  -- Adding: Yvan Babillon
  try
    set newContact to make new person with properties {first name:"Yvan", last name:"Babillon", organization:""}
    make new email at end of emails of newContact with properties {label:"work", value:"ybabillon@gmail.com"}
    set addedCount to addedCount + 1
  on error
    -- Skip if already exists
  end try

  -- Adding: Yariela Aguero
  try
    set newContact to make new person with properties {first name:"Yariela", last name:"Aguero", organization:""}
    make new email at end of emails of newContact with properties {label:"work", value:"yariela.aguero@experian.com"}
    set addedCount to addedCount + 1
  on error
    -- Skip if already exists
  end try

  -- Adding: Yusuke Tan
  try
    set newContact to make new person with properties {first name:"Yusuke", last name:"Tan", organization:""}
    make new email at end of emails of newContact with properties {label:"work", value:"usk@usk.cc"}
    set addedCount to addedCount + 1
  on error
    -- Skip if already exists
  end try

  -- Adding: Wolfgang von Stuermer
  try
    set newContact to make new person with properties {first name:"Wolfgang", last name:"von Stuermer", organization:""}
    make new email at end of emails of newContact with properties {label:"work", value:"wvs@computer.org"}
    set addedCount to addedCount + 1
  on error
    -- Skip if already exists
  end try

  -- Adding: Wizwars
  try
    set newContact to make new person with properties {first name:"Wizwars", last name:"", organization:""}
    make new email at end of emails of newContact with properties {label:"work", value:"wizwars@gmail.com"}
    set addedCount to addedCount + 1
  on error
    -- Skip if already exists
  end try

  -- Adding: Zach Weir
  try
    set newContact to make new person with properties {first name:"Zach", last name:"Weir", organization:""}
    make new email at end of emails of newContact with properties {label:"work", value:"zach.weir@signify-tech.com"}
    set addedCount to addedCount + 1
  on error
    -- Skip if already exists
  end try

  -- Adding: Yaz Leal
  try
    set newContact to make new person with properties {first name:"Yaz", last name:"Leal", organization:""}
    make new email at end of emails of newContact with properties {label:"work", value:"gamiyaz@hotmail.com"}
    set addedCount to addedCount + 1
  on error
    -- Skip if already exists
  end try

  -- Adding: Yuko Coova
  try
    set newContact to make new person with properties {first name:"Yuko", last name:"Coova", organization:""}
    make new email at end of emails of newContact with properties {label:"work", value:"coovach@gmail.com"}
    set addedCount to addedCount + 1
  on error
    -- Skip if already exists
  end try

  -- Adding: xlmuertelx xlmuertelx
  try
    set newContact to make new person with properties {first name:"xlmuertelx", last name:"xlmuertelx", organization:""}
    make new email at end of emails of newContact with properties {label:"work", value:"xlmuertelx@hotmail.com"}
    set addedCount to addedCount + 1
  on error
    -- Skip if already exists
  end try

  -- Adding: Adam Garling
  try
    set newContact to make new person with properties {first name:"Adam", last name:"Garling", organization:""}
    make new email at end of emails of newContact with properties {label:"work", value:"agarling@asapp.com"}
    set addedCount to addedCount + 1
  on error
    -- Skip if already exists
  end try

  -- Adding: tsacco@vettery.com
  try
    set newContact to make new person with properties {first name:"tsacco@vettery.com", last name:"", organization:""}
    make new email at end of emails of newContact with properties {label:"work", value:"tsacco@vettery.com"}
    set addedCount to addedCount + 1
  on error
    -- Skip if already exists
  end try

  -- Adding: Kristin Wagonblott
  try
    set newContact to make new person with properties {first name:"Kristin", last name:"Wagonblott", organization:""}
    make new email at end of emails of newContact with properties {label:"work", value:"kristinannvirtualsolutions@gmail.com"}
    set addedCount to addedCount + 1
  on error
    -- Skip if already exists
  end try

  -- Adding: bleo@dummydrome.com
  try
    set newContact to make new person with properties {first name:"bleo@dummydrome.com", last name:"", organization:""}
    make new email at end of emails of newContact with properties {label:"work", value:"bleo@dummydrome.com"}
    set addedCount to addedCount + 1
  on error
    -- Skip if already exists
  end try

  -- Adding: Jill Reamer
  try
    set newContact to make new person with properties {first name:"Jill", last name:"Reamer", organization:""}
    make new email at end of emails of newContact with properties {label:"work", value:"jreamer@peakpartnersusa.com"}
    make new phone at end of phones of newContact with properties {label:"work", value:"(410) 428-6585"}
    set addedCount to addedCount + 1
  on error
    -- Skip if already exists
  end try

  -- Adding: Ketty Davis
  try
    set newContact to make new person with properties {first name:"Ketty", last name:"Davis", organization:""}
    make new email at end of emails of newContact with properties {label:"work", value:"hargix@yahoo.com"}
    set addedCount to addedCount + 1
  on error
    -- Skip if already exists
  end try

  -- Adding: marieke
  try
    set newContact to make new person with properties {first name:"marieke", last name:"", organization:""}
    make new email at end of emails of newContact with properties {label:"work", value:"marieke@marieke.nu"}
    set addedCount to addedCount + 1
  on error
    -- Skip if already exists
  end try

  -- Adding: Euan Lynn
  try
    set newContact to make new person with properties {first name:"Euan", last name:"Lynn", organization:""}
    make new email at end of emails of newContact with properties {label:"work", value:"tracekaiser@yahoo.co.uk"}
    set addedCount to addedCount + 1
  on error
    -- Skip if already exists
  end try

  -- Adding: Jordan Ring
  try
    set newContact to make new person with properties {first name:"Jordan", last name:"Ring", organization:""}
    make new email at end of emails of newContact with properties {label:"work", value:"jordanring7@gmail.com"}
    set addedCount to addedCount + 1
  on error
    -- Skip if already exists
  end try

  -- Adding: Jerry Coox
  try
    set newContact to make new person with properties {first name:"Jerry", last name:"Coox", organization:""}
    make new email at end of emails of newContact with properties {label:"work", value:"woolyss@live.com"}
    set addedCount to addedCount + 1
  on error
    -- Skip if already exists
  end try

  -- Adding: Dermot Jelfs
  try
    set newContact to make new person with properties {first name:"Dermot", last name:"Jelfs", organization:""}
    make new email at end of emails of newContact with properties {label:"work", value:"neon_love@hotmail.co.uk"}
    set addedCount to addedCount + 1
  on error
    -- Skip if already exists
  end try

  -- Adding: Ben Byford
  try
    set newContact to make new person with properties {first name:"Ben", last name:"Byford", organization:""}
    make new email at end of emails of newContact with properties {label:"work", value:"william.byford@walmart.com"}
    set addedCount to addedCount + 1
  on error
    -- Skip if already exists
  end try

  -- Adding: Ron & Jeri Mendel
  try
    set newContact to make new person with properties {first name:"Ron", last name:"& Jeri Mendel", organization:""}
    make new email at end of emails of newContact with properties {label:"work", value:"ronaldmendel@gmail.com"}
    set addedCount to addedCount + 1
  on error
    -- Skip if already exists
  end try

  -- Adding: Louis Michael Weisbach
  try
    set newContact to make new person with properties {first name:"Louis", last name:"Michael Weisbach", organization:""}
    make new email at end of emails of newContact with properties {label:"work", value:"loumichael@premiumsolution.com"}
    set addedCount to addedCount + 1
  on error
    -- Skip if already exists
  end try

  -- Adding: iloveui. com
  try
    set newContact to make new person with properties {first name:"iloveui.", last name:"com", organization:""}
    make new email at end of emails of newContact with properties {label:"work", value:"info@iloveui.com"}
    set addedCount to addedCount + 1
  on error
    -- Skip if already exists
  end try

  -- Adding: Andrea & Howard Finkle
  try
    set newContact to make new person with properties {first name:"Andrea", last name:"& Howard Finkle", organization:""}
    make new email at end of emails of newContact with properties {label:"work", value:"andreafinkle@hotmail.com"}
    set addedCount to addedCount + 1
  on error
    -- Skip if already exists
  end try

  -- Adding: Kyle Sherrod
  try
    set newContact to make new person with properties {first name:"Kyle", last name:"Sherrod", organization:""}
    make new email at end of emails of newContact with properties {label:"work", value:"pasoconotaku@hotmail.com"}
    set addedCount to addedCount + 1
  on error
    -- Skip if already exists
  end try

  -- Adding: Justin Siegel
  try
    set newContact to make new person with properties {first name:"Justin", last name:"Siegel", organization:""}
    make new email at end of emails of newContact with properties {label:"work", value:"moconinja@gmail.com"}
    set addedCount to addedCount + 1
  on error
    -- Skip if already exists
  end try

  -- Adding: Rachael Parekh
  try
    set newContact to make new person with properties {first name:"Rachael", last name:"Parekh", organization:""}
    make new email at end of emails of newContact with properties {label:"work", value:"rjanedoe@gmail.com"}
    set addedCount to addedCount + 1
  on error
    -- Skip if already exists
  end try

  -- Adding: Grayson Shroyer
  try
    set newContact to make new person with properties {first name:"Grayson", last name:"Shroyer", organization:""}
    make new email at end of emails of newContact with properties {label:"work", value:"grayson.shroyer@microsoft.com"}
    set addedCount to addedCount + 1
  on error
    -- Skip if already exists
  end try

  -- Adding: Alex Yabsley
  try
    set newContact to make new person with properties {first name:"Alex", last name:"Yabsley", organization:""}
    make new email at end of emails of newContact with properties {label:"work", value:"alex_yabsley@hotmail.com"}
    set addedCount to addedCount + 1
  on error
    -- Skip if already exists
  end try

  -- Adding: Mary Ann Benedetto
  try
    set newContact to make new person with properties {first name:"Mary", last name:"Ann Benedetto", organization:""}
    make new email at end of emails of newContact with properties {label:"work", value:"outpt@mac.com"}
    set addedCount to addedCount + 1
  on error
    -- Skip if already exists
  end try

  -- Adding: Greg Boyle
  try
    set newContact to make new person with properties {first name:"Greg", last name:"Boyle", organization:""}
    make new email at end of emails of newContact with properties {label:"work", value:"gregory.boyle@opco.com"}
    set addedCount to addedCount + 1
  on error
    -- Skip if already exists
  end try

  -- Adding: sulumi
  try
    set newContact to make new person with properties {first name:"sulumi", last name:"", organization:""}
    make new email at end of emails of newContact with properties {label:"work", value:"sulumi@gmail.com"}
    set addedCount to addedCount + 1
  on error
    -- Skip if already exists
  end try

  -- Adding: Doug Baum
  try
    set newContact to make new person with properties {first name:"Doug", last name:"Baum", organization:""}
    make new email at end of emails of newContact with properties {label:"work", value:"doug@baumrealty.com"}
    set addedCount to addedCount + 1
  on error
    -- Skip if already exists
  end try

  -- Adding: Neil Voss
  try
    set newContact to make new person with properties {first name:"Neil", last name:"Voss", organization:""}
    make new email at end of emails of newContact with properties {label:"work", value:"voss@alinear.net"}
    set addedCount to addedCount + 1
  on error
    -- Skip if already exists
  end try

  -- Adding: Jennifer Hartwick
  try
    set newContact to make new person with properties {first name:"Jennifer", last name:"Hartwick", organization:""}
    make new email at end of emails of newContact with properties {label:"work", value:"jennifer@revunit.com"}
    set addedCount to addedCount + 1
  on error
    -- Skip if already exists
  end try

  -- Adding: Indigo Rose
  try
    set newContact to make new person with properties {first name:"Indigo", last name:"Rose", organization:""}
    make new email at end of emails of newContact with properties {label:"work", value:"irose@leejohnson.com"}
    set addedCount to addedCount + 1
  on error
    -- Skip if already exists
  end try

  -- Adding: Richard Millman
  try
    set newContact to make new person with properties {first name:"Richard", last name:"Millman", organization:""}
    make new email at end of emails of newContact with properties {label:"work", value:"richard.millman68@gmail.com"}
    set addedCount to addedCount + 1
  on error
    -- Skip if already exists
  end try

  -- Adding: Will Adams
  try
    set newContact to make new person with properties {first name:"Will", last name:"Adams", organization:""}
    make new email at end of emails of newContact with properties {label:"work", value:"will.adams@randstadusa.com"}
    set addedCount to addedCount + 1
  on error
    -- Skip if already exists
  end try

  -- Adding: Linda Jean Ungar
  try
    set newContact to make new person with properties {first name:"Linda", last name:"Jean Ungar", organization:""}
    make new email at end of emails of newContact with properties {label:"work", value:"linnyj144@aol.com"}
    set addedCount to addedCount + 1
  on error
    -- Skip if already exists
  end try

  -- Adding: Kara Moore
  try
    set newContact to make new person with properties {first name:"Kara", last name:"Moore", organization:""}
    make new email at end of emails of newContact with properties {label:"work", value:"kara_moore@apple.com"}
    set addedCount to addedCount + 1
  on error
    -- Skip if already exists
  end try

  -- Adding: Daniel Arena
  try
    set newContact to make new person with properties {first name:"Daniel", last name:"Arena", organization:""}
    make new email at end of emails of newContact with properties {label:"work", value:"dan@remaincalm.org"}
    set addedCount to addedCount + 1
  on error
    -- Skip if already exists
  end try

  -- Adding: Katie Knutson
  try
    set newContact to make new person with properties {first name:"Katie", last name:"Knutson", organization:""}
    make new email at end of emails of newContact with properties {label:"work", value:"kknutson@vaynermedia.com"}
    set addedCount to addedCount + 1
  on error
    -- Skip if already exists
  end try

  -- Adding: Michael Dressler
  try
    set newContact to make new person with properties {first name:"Michael", last name:"Dressler", organization:""}
    make new email at end of emails of newContact with properties {label:"work", value:"dressler.michael@soestcom.biz"}
    set addedCount to addedCount + 1
  on error
    -- Skip if already exists
  end try

  -- Adding: Hayley Scanlon
  try
    set newContact to make new person with properties {first name:"Hayley", last name:"Scanlon", organization:""}
    make new email at end of emails of newContact with properties {label:"work", value:"hayleyscanlon11@gmail.com"}
    set addedCount to addedCount + 1
  on error
    -- Skip if already exists
  end try

  -- Adding: Sebastian Hennig
  try
    set newContact to make new person with properties {first name:"Sebastian", last name:"Hennig", organization:""}
    make new email at end of emails of newContact with properties {label:"work", value:"hennig2@googlemail.com"}
    set addedCount to addedCount + 1
  on error
    -- Skip if already exists
  end try

  -- Adding: Katherine Kassens
  try
    set newContact to make new person with properties {first name:"Katherine", last name:"Kassens", organization:""}
    make new email at end of emails of newContact with properties {label:"work", value:"kkassens@liveperson.com"}
    set addedCount to addedCount + 1
  on error
    -- Skip if already exists
  end try

  -- Adding: Dakota Clark
  try
    set newContact to make new person with properties {first name:"Dakota", last name:"Clark", organization:""}
    make new email at end of emails of newContact with properties {label:"work", value:"dakotamusic@gmail.com"}
    set addedCount to addedCount + 1
  on error
    -- Skip if already exists
  end try

  -- Adding: Debra Michelle Wineberg
  try
    set newContact to make new person with properties {first name:"Debra", last name:"Michelle Wineberg", organization:""}
    make new email at end of emails of newContact with properties {label:"work", value:"dmw52555@aol.com"}
    set addedCount to addedCount + 1
  on error
    -- Skip if already exists
  end try

  -- Adding: Nick VanHolt
  try
    set newContact to make new person with properties {first name:"Nick", last name:"VanHolt", organization:""}
    make new email at end of emails of newContact with properties {label:"work", value:"dise321@gmail.com"}
    set addedCount to addedCount + 1
  on error
    -- Skip if already exists
  end try

  -- Adding: Naji Hendrix
  try
    set newContact to make new person with properties {first name:"Naji", last name:"Hendrix", organization:""}
    make new email at end of emails of newContact with properties {label:"work", value:"naji.hendrix@cybercoders.com"}
    set addedCount to addedCount + 1
  on error
    -- Skip if already exists
  end try

  -- Adding: Henry Owings
  try
    set newContact to make new person with properties {first name:"Henry", last name:"Owings", organization:""}
    make new email at end of emails of newContact with properties {label:"work", value:"henry@chunklet.com"}
    set addedCount to addedCount + 1
  on error
    -- Skip if already exists
  end try

  -- Adding: Obaid Ahmed
  try
    set newContact to make new person with properties {first name:"Obaid", last name:"Ahmed", organization:""}
    make new email at end of emails of newContact with properties {label:"work", value:"obaid@botmock.com"}
    set addedCount to addedCount + 1
  on error
    -- Skip if already exists
  end try

  -- Adding: Andrew Reitano
  try
    set newContact to make new person with properties {first name:"Andrew", last name:"Reitano", organization:""}
    make new email at end of emails of newContact with properties {label:"work", value:"areitan1@gmail.com"}
    make new phone at end of phones of newContact with properties {label:"work", value:"5165932827"}
    set addedCount to addedCount + 1
  on error
    -- Skip if already exists
  end try

  -- Adding: William Rickman
  try
    set newContact to make new person with properties {first name:"William", last name:"Rickman", organization:""}
    make new email at end of emails of newContact with properties {label:"work", value:"william.rickman@gmail.com"}
    set addedCount to addedCount + 1
  on error
    -- Skip if already exists
  end try

  -- Adding: Angela Long
  try
    set newContact to make new person with properties {first name:"Angela", last name:"Long", organization:""}
    make new email at end of emails of newContact with properties {label:"work", value:"along2@godaddy.com"}
    set addedCount to addedCount + 1
  on error
    -- Skip if already exists
  end try

  -- Adding: Lars Lewerenz (Audiolith Records)
  try
    set newContact to make new person with properties {first name:"Lars", last name:"Lewerenz (Audiolith Records)", organization:""}
    make new email at end of emails of newContact with properties {label:"work", value:"lars@audiolith.net"}
    set addedCount to addedCount + 1
  on error
    -- Skip if already exists
  end try

  -- Adding: Jonny Nero
  try
    set newContact to make new person with properties {first name:"Jonny", last name:"Nero", organization:""}
    make new email at end of emails of newContact with properties {label:"work", value:"jonnynero@gmail.com"}
    set addedCount to addedCount + 1
  on error
    -- Skip if already exists
  end try

  -- Adding: Eric Speer
  try
    set newContact to make new person with properties {first name:"Eric", last name:"Speer", organization:""}
    make new email at end of emails of newContact with properties {label:"work", value:"eespeer@gmail.com"}
    set addedCount to addedCount + 1
  on error
    -- Skip if already exists
  end try

  -- Adding: Colleen Harvey
  try
    set newContact to make new person with properties {first name:"Colleen", last name:"Harvey", organization:""}
    make new email at end of emails of newContact with properties {label:"work", value:"colleen.harvey.wzqe@statefarm.com"}
    set addedCount to addedCount + 1
  on error
    -- Skip if already exists
  end try

  -- Adding: Stuart Soloway
  try
    set newContact to make new person with properties {first name:"Stuart", last name:"Soloway", organization:""}
    make new email at end of emails of newContact with properties {label:"work", value:"stuartatril@aol.com"}
    set addedCount to addedCount + 1
  on error
    -- Skip if already exists
  end try

  -- Adding: Matthew Stearn
  try
    set newContact to make new person with properties {first name:"Matthew", last name:"Stearn", organization:""}
    make new email at end of emails of newContact with properties {label:"work", value:"matthew.stearn@seilevel.com"}
    set addedCount to addedCount + 1
  on error
    -- Skip if already exists
  end try

  -- Adding: Janet Ring
  try
    set newContact to make new person with properties {first name:"Janet", last name:"Ring", organization:""}
    make new email at end of emails of newContact with properties {label:"work", value:"jgring1@aol.com"}
    set addedCount to addedCount + 1
  on error
    -- Skip if already exists
  end try

  -- Adding: Dan Theman
  try
    set newContact to make new person with properties {first name:"Dan", last name:"Theman", organization:""}
    make new email at end of emails of newContact with properties {label:"work", value:"danimal201@yahoo.com"}
    set addedCount to addedCount + 1
  on error
    -- Skip if already exists
  end try

  -- Adding: Bas Steendijk
  try
    set newContact to make new person with properties {first name:"Bas", last name:"Steendijk", organization:""}
    make new email at end of emails of newContact with properties {label:"work", value:"beware@bircd.org"}
    set addedCount to addedCount + 1
  on error
    -- Skip if already exists
  end try

  -- Adding: Emily Ryan
  try
    set newContact to make new person with properties {first name:"Emily", last name:"Ryan", organization:""}
    make new email at end of emails of newContact with properties {label:"work", value:"emilyanneryan@gmail.com"}
    set addedCount to addedCount + 1
  on error
    -- Skip if already exists
  end try

  -- Adding: Britnee Laughlin
  try
    set newContact to make new person with properties {first name:"Britnee", last name:"Laughlin", organization:""}
    make new email at end of emails of newContact with properties {label:"work", value:"blaughlin@drift.com"}
    set addedCount to addedCount + 1
  on error
    -- Skip if already exists
  end try

  -- Adding: Andre ZP
  try
    set newContact to make new person with properties {first name:"Andre", last name:"ZP", organization:""}
    make new email at end of emails of newContact with properties {label:"work", value:"andrezp@gmx.de"}
    set addedCount to addedCount + 1
  on error
    -- Skip if already exists
  end try

  -- Adding: Peter Rojas
  try
    set newContact to make new person with properties {first name:"Peter", last name:"Rojas", organization:""}
    make new email at end of emails of newContact with properties {label:"work", value:"peter@betaworks.com"}
    set addedCount to addedCount + 1
  on error
    -- Skip if already exists
  end try

  -- Adding: alicefelldown
  try
    set newContact to make new person with properties {first name:"alicefelldown", last name:"", organization:""}
    make new email at end of emails of newContact with properties {label:"work", value:"alicefelldown@gmail.com"}
    set addedCount to addedCount + 1
  on error
    -- Skip if already exists
  end try

  -- Adding: Kevin James
  try
    set newContact to make new person with properties {first name:"Kevin", last name:"James", organization:""}
    make new email at end of emails of newContact with properties {label:"work", value:"dangerjames@earthlink.net"}
    set addedCount to addedCount + 1
  on error
    -- Skip if already exists
  end try

  -- Adding: Del Baker
  try
    set newContact to make new person with properties {first name:"Del", last name:"Baker", organization:""}
    make new email at end of emails of newContact with properties {label:"work", value:"del.baker@gartner.com"}
    set addedCount to addedCount + 1
  on error
    -- Skip if already exists
  end try

  -- Adding: Scott Rosen
  try
    set newContact to make new person with properties {first name:"Scott", last name:"Rosen", organization:""}
    make new email at end of emails of newContact with properties {label:"work", value:"scottarosen@hotmail.com"}
    set addedCount to addedCount + 1
  on error
    -- Skip if already exists
  end try

  -- Adding: Gaby Rugelio
  try
    set newContact to make new person with properties {first name:"Gaby", last name:"Rugelio", organization:""}
    make new email at end of emails of newContact with properties {label:"work", value:"admin@trehomes.com"}
    set addedCount to addedCount + 1
  on error
    -- Skip if already exists
  end try

  -- Adding: Matt Marshall
  try
    set newContact to make new person with properties {first name:"Matt", last name:"Marshall", organization:""}
    make new email at end of emails of newContact with properties {label:"work", value:"mmarshall@venturebeat.com"}
    set addedCount to addedCount + 1
  on error
    -- Skip if already exists
  end try

  -- Adding: Piper Ronz
  try
    set newContact to make new person with properties {first name:"Piper", last name:"Ronz", organization:""}
    make new email at end of emails of newContact with properties {label:"work", value:"pmronz@yahoo.com"}
    set addedCount to addedCount + 1
  on error
    -- Skip if already exists
  end try

  -- Adding: Amber Land
  try
    set newContact to make new person with properties {first name:"Amber", last name:"Land", organization:""}
    make new email at end of emails of newContact with properties {label:"work", value:"al.lppestsolutions@gmail.com"}
    set addedCount to addedCount + 1
  on error
    -- Skip if already exists
  end try

  -- Adding: Jill Soloway
  try
    set newContact to make new person with properties {first name:"Jill", last name:"Soloway", organization:""}
    make new email at end of emails of newContact with properties {label:"work", value:"jillwaysolo@gmail.com"}
    set addedCount to addedCount + 1
  on error
    -- Skip if already exists
  end try

  -- Adding: Alex Jury
  try
    set newContact to make new person with properties {first name:"Alex", last name:"Jury", organization:""}
    make new email at end of emails of newContact with properties {label:"work", value:"a.jury@roc-search.us"}
    set addedCount to addedCount + 1
  on error
    -- Skip if already exists
  end try

  -- Adding: Elaina ten Bosch
  try
    set newContact to make new person with properties {first name:"Elaina", last name:"ten Bosch", organization:""}
    make new email at end of emails of newContact with properties {label:"work", value:"e10bosch@gmail.com"}
    set addedCount to addedCount + 1
  on error
    -- Skip if already exists
  end try

  -- Adding: Johan Larsby
  try
    set newContact to make new person with properties {first name:"Johan", last name:"Larsby", organization:""}
    make new email at end of emails of newContact with properties {label:"work", value:"johan.larsby@gmail.com"}
    set addedCount to addedCount + 1
  on error
    -- Skip if already exists
  end try

  -- Adding: Dominique Whittaker
  try
    set newContact to make new person with properties {first name:"Dominique", last name:"Whittaker", organization:""}
    make new email at end of emails of newContact with properties {label:"work", value:"dwhittaker@pacific-crest.com"}
    set addedCount to addedCount + 1
  on error
    -- Skip if already exists
  end try

  -- Adding: ben coullie
  try
    set newContact to make new person with properties {first name:"ben", last name:"coullie", organization:""}
    make new email at end of emails of newContact with properties {label:"work", value:"cityscapego@gmail.com"}
    set addedCount to addedCount + 1
  on error
    -- Skip if already exists
  end try

  -- Adding: Marcy 
  try
    set newContact to make new person with properties {first name:"Marcy", last name:"Dqt", organization:""}
    make new email at end of emails of newContact with properties {label:"work", value:"marcydqt@sbcglobal.net"}
    set addedCount to addedCount + 1
  on error
    -- Skip if already exists
  end try

  -- Adding: Asif Siddiky
  try
    set newContact to make new person with properties {first name:"Asif", last name:"Siddiky", organization:""}
    make new email at end of emails of newContact with properties {label:"work", value:"asifsiddiky@gmail.com"}
    make new phone at end of phones of newContact with properties {label:"work", value:"+17576354591"}
    set addedCount to addedCount + 1
  on error
    -- Skip if already exists
  end try

  -- Adding: Orrett Davis
  try
    set newContact to make new person with properties {first name:"Orrett", last name:"Davis", organization:""}
    make new email at end of emails of newContact with properties {label:"work", value:"orrett_old@abe.ai"}
    set addedCount to addedCount + 1
  on error
    -- Skip if already exists
  end try

  -- Adding: 🎀 Rae
  try
    set newContact to make new person with properties {first name:"🎀", last name:"Rae", organization:""}
    make new email at end of emails of newContact with properties {label:"work", value:"partytimehexcellent@gmail.com"}
    make new phone at end of phones of newContact with properties {label:"work", value:"+14698676577"}
    set addedCount to addedCount + 1
  on error
    -- Skip if already exists
  end try

  -- Adding: Chelsea Koehler
  try
    set newContact to make new person with properties {first name:"Chelsea", last name:"Koehler", organization:""}
    make new email at end of emails of newContact with properties {label:"work", value:"chelsea@rev.com"}
    set addedCount to addedCount + 1
  on error
    -- Skip if already exists
  end try

  -- Adding: Anthony Costello
  try
    set newContact to make new person with properties {first name:"Anthony", last name:"Costello", organization:""}
    make new email at end of emails of newContact with properties {label:"work", value:"a.costello@austinfraser.com"}
    set addedCount to addedCount + 1
  on error
    -- Skip if already exists
  end try

  -- Adding: Sallee Gambino
  try
    set newContact to make new person with properties {first name:"Sallee", last name:"Gambino", organization:""}
    make new email at end of emails of newContact with properties {label:"work", value:"sallee_gambino@intuit.com"}
    set addedCount to addedCount + 1
  on error
    -- Skip if already exists
  end try

  -- Adding: Kevin C. Smith
  try
    set newContact to make new person with properties {first name:"Kevin", last name:"C. Smith", organization:""}
    make new email at end of emails of newContact with properties {label:"work", value:"kevincsmith9@gmail.com"}
    set addedCount to addedCount + 1
  on error
    -- Skip if already exists
  end try

  -- Adding: Robin Pierucci
  try
    set newContact to make new person with properties {first name:"Robin", last name:"Pierucci", organization:""}
    make new email at end of emails of newContact with properties {label:"work", value:"pierucci726@sbcglobal.net"}
    set addedCount to addedCount + 1
  on error
    -- Skip if already exists
  end try

  -- Adding: Brad Methe
  try
    set newContact to make new person with properties {first name:"Brad", last name:"Methe", organization:""}
    make new email at end of emails of newContact with properties {label:"work", value:"bradjm33@hotmail.com"}
    set addedCount to addedCount + 1
  on error
    -- Skip if already exists
  end try

  -- Adding: David Armando Cabrera Ortiz
  try
    set newContact to make new person with properties {first name:"David", last name:"Armando Cabrera Ortiz", organization:""}
    make new email at end of emails of newContact with properties {label:"work", value:"david.cabrera@gmodelo.com.mx"}
    set addedCount to addedCount + 1
  on error
    -- Skip if already exists
  end try

  -- Adding: Sydney Plumey
  try
    set newContact to make new person with properties {first name:"Sydney", last name:"Plumey", organization:""}
    make new email at end of emails of newContact with properties {label:"work", value:"ultrasyd@hotmail.com"}
    set addedCount to addedCount + 1
  on error
    -- Skip if already exists
  end try

  -- Adding: Danny Castaneda
  try
    set newContact to make new person with properties {first name:"Danny", last name:"Castaneda", organization:""}
    make new email at end of emails of newContact with properties {label:"work", value:"dcastaneda@ahfh.org"}
    set addedCount to addedCount + 1
  on error
    -- Skip if already exists
  end try

  -- Adding: Michelle Sternberger
  try
    set newContact to make new person with properties {first name:"Michelle", last name:"Sternberger", organization:""}
    make new email at end of emails of newContact with properties {label:"work", value:"computeher@gmail.com"}
    make new phone at end of phones of newContact with properties {label:"work", value:"1-562-746-6294"}
    set addedCount to addedCount + 1
  on error
    -- Skip if already exists
  end try

  -- Adding: Rich Vreeland
  try
    set newContact to make new person with properties {first name:"Rich", last name:"Vreeland", organization:""}
    make new email at end of emails of newContact with properties {label:"work", value:"rich.vreeland@gmail.com"}
    set addedCount to addedCount + 1
  on error
    -- Skip if already exists
  end try

  display dialog "Successfully added " & addedCount & " new contacts to Apple Contacts!"
end tell